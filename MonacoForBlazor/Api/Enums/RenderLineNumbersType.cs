namespace MonacoForBlazor.Api.Enums
{
    /// <summary>
    /// Determines how line numbers are rendered.
    /// </summary>
    public enum RenderLineNumbersType
    {
        Off = 0,
        On = 1,
        Relative = 2,
        Interval = 3,
        Custom = 4
    }
}
